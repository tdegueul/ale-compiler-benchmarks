behavior printexp;

import ecore "platform:/resource/boolexp/model/BoolExp.ecore";

open class Tru {
	def String print() {
		return "T";
	}
}

open class Fals {
	def String print() {
		return "F";
	}
}

open class And {
	def String print() {
		return "(" + $[self.getLhs()].print() + " ∧ " + $[self.getRhs()].print() + ")";
	}
}

open class Or {
	def String print() {
		return "(" + $[self.getLhs()].print() + " ∨ " + $[self.getRhs()].print() + ")";
	}
}

open class Exp {
	def String print() { return ""; }
}

open class BinaryExp {
	def String print() { return ""; }
}

open class Lit {
	def String print() { return ""; }
}
